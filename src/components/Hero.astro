---
// src/components/Hero.astro
---
<section id="hero" class="relative h-[80vh] overflow-hidden flex items-center justify-center">
  <!-- Video de fondo -->
  <video
    autoplay
    muted
    loop
    playsinline
    class="absolute w-full h-full object-cover"
    src="/videos/ehome-Institucional-Renoair.mp4"
  ></video>

  <!-- Capa oscura -->
  <div class="absolute inset-0 bg-black/40"></div>

  <!-- Contenido -->
  <div
    id="hero-content"
    class="relative z-10 text-center text-white px-4 max-w-2xl"
  >
    <h1
      class="text-5xl md:text-6xl font-extrabold tracking-tight leading-snug
             bg-clip-text text-transparent bg-gradient-to-r 
             from-[var(--arg-accent)] to-[var(--arg-primary)]
             drop-shadow-lg"
    >
      ¡TODO SUCEDE<br />CUANDO EL VIENTO SOPLA!
    </h1>
    <p class="mt-4 text-base md:text-lg text-[var(--arg-neutral)]">
      Somos apasionados por el viento, transformamos datos en energía.
    </p>
    <a
      href="#contacto"
      class="inline-flex items-center px-4 py-2 mt-6 
             bg-[var(--arg-accent)] hover:bg-[var(--arg-primary)] 
             text-white text-base font-semibold rounded-md shadow-lg transition"
    >
      Contáctanos
      <span class="ml-1 text-xl">💬</span>
    </a>
  </div>

  <!-- Animación al load -->
  <script type="module" is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const hero = document.getElementById('hero-content');
      const items = hero.querySelectorAll('h1, p, a');
      items.forEach((el, i) => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(1rem)';
        setTimeout(() => {
          el.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
          el.style.opacity = '1';
          el.style.transform = 'translateY(0)';
        }, 200 + i * 200);
      });
    });
  </script>
</section>
