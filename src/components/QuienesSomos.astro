---
import { Image } from 'astro:assets';
---
<section id="quienes-somos" class="relative overflow-hidden pt-20 pb-32 bg-[var(--arg-primary)] ">
  <!-- Top wave -->
  <div class="absolute top-0 left-0 w-full overflow-hidden leading-none">
    <svg class="relative block w-full h-20" viewBox="0 0 1440 320" preserveAspectRatio="none">
      <path
        fill="var(--arg-primary)"
        d="M0,160L48,144C96,128,192,96,288,96C384,96,480,128,576,160C672,192,768,224,864,224C960,224,1056,192,1152,160C1248,128,1344,96,1392,80L1440,64L1440,0L1392,0C1344,0,1248,0,1152,0C1056,0,960,0,864,0C768,0,672,0,576,0C480,0,384,0,288,0C192,0,96,0,48,0L0,0Z"
      />
    </svg>
  </div>

  <div class="relative z-10 max-w-screen-lg mx-auto px-4 grid md:grid-cols-2 gap-12 items-center">
    <!-- Imagen con slide-in desde la izquierda -->
    <figure
      data-animate
      class="opacity-0 transform -translate-x-6 transition-all duration-700 ease-out relative overflow-hidden rounded-xl"
    >
      <Image
        src="/images/logo-dark-renoair.png.webp"
        alt="Equipo de RenoAir"
        class="w-full h-auto object-cover transform transition-transform duration-700 group-hover:scale-110"
        width={400}
        height={500}
        loading="lazy"
      />
      <div class=" animate-pulse-slow"></div>
    </figure>

    <!-- Texto con slide-in desde la derecha -->
    <div
      data-animate
      class="opacity-0 transform translate-x-6 transition-all duration-700 ease-out"
    >
      <span class="text-sm font-semibold uppercase text-[var(--arg-accent)]">Bienvenidos</span>
      <h2 class="mt-2 text-4xl md:text-5xl font-bold uppercase text-[var(--arg-accent)] inline-block relative">
        Quiénes somos
        <span class="absolute bottom-0 left-0 block h-1 w-full bg-[var(--arg-accent)] scale-x-0 origin-left animate-draw"></span>
      </h2>
      <p class="mt-6 text-lg leading-relaxed text-white">
        Somos una empresa 100% brasileña con 10 años en el mercado de la Energía Eólica, trabajando con socios nacionales e internacionales. Especialistas en mediciones anemométricas, ofrecemos consultoría y asesoría técnica desde la prospección hasta la implementación de parques eólicos, y fabricamos estructuras y equipos para energías renovables.
      </p>
      <p class="mt-4 text-lg leading-relaxed text-white">
        Nuestro equipo de ingenieros apasionados crea soluciones sostenibles que impulsan el futuro de la energía limpia.
      </p>
      <a
        href="/nosotros"
        class="inline-block mt-8 px-8 py-3 bg-[var(--arg-accent)] hover:bg-[var(--arg-primary)] text-white font-semibold rounded-full transition-colors"
      >
        Saber más ➔
      </a>
    </div>
  </div>

  <!-- Bottom wave -->
  <div class="absolute bottom-0 left-0 w-full overflow-hidden leading-none rotate-180">
    <svg class="relative block w-full h-20" viewBox="0 0 1440 320" preserveAspectRatio="none">
      <path
        fill="var(--arg-accent)"
        d="M0,160L48,144C96,128,192,96,288,96C384,96,480,128,576,160C672,192,768,224,864,224C960,224,1056,192,1152,160C1248,128,1344,96,1392,80L1440,64L1440,0L1392,0C1344,0,1248,0,1152,0C1056,0,960,0,864,0C768,0,672,0,576,0C480,0,384,0,288,0C192,0,96,0,48,0L0,0Z"
      />
    </svg>
  </div>
  

  <script type="module" is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const section = document.getElementById('quienes-somos');
      const animated = section.querySelectorAll('[data-animate]');
      const io = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.remove('opacity-0', 'translate-x-6', '-translate-x-6');
            obs.unobserve(entry.target);
          }
        });
      }, { threshold: 0.3 });
      animated.forEach(el => io.observe(el));
    });
  </script>

  <style is:inline>
    @keyframes draw { to { transform: scaleX(1) } }
    .animate-draw { animation: draw 0.8s ease-out forwards; }
    @keyframes pulse { 0%,100% { opacity:0.2 } 50% { opacity:0.4 } }
    .animate-pulse-slow { animation: pulse 3s ease-in-out infinite; }
  </style>
</section>
